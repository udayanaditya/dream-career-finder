
import React from 'react';

// This is a placeholder component to fix the build error
// It's not actually functional since we've removed the theme switching
const ThemeSwitcher = () => {
  return null;
};

export default ThemeSwitcher;
